# 手机端横屏提词器 —— 架构文档(第一版)

> 本文件是给开发者(及 AI 工具 Codex)的**唯一权威实现依据**。请严格按本文档实现第一版,
> 不要添加任何未在「功能范围」中列出的功能。架构由架构师负责,代码由 Codex 负责,代码完成后由架构师审查。

---

## 0. 一句话定位

一个**网页版提词器**:在手机浏览器里**横屏**打开,贴入稿子,文字匀速向上滚动,跟着念。
第一版**只在本地浏览器跑通**,无部署、无后端、无账号。

---

## 1. 第一版功能范围(严格遵守,不要多做)

1. **文本输入**:一个区域,可贴入/编辑稿子文本。
2. **匀速自动滚动**:点击开始,文字从下往上匀速滚动;可暂停/继续;可重置到开头。
3. **镜像翻转(核心)**:一个开关,开启后整段滚动文字做**水平镜像翻转**(左右翻转,用于提词器反光玻璃)。必须有,一键开关。
4. **字号可调**:实时调整文字大小(滑块或加减按钮)。
5. **滚动速度可调**:实时调整,范围足够宽(能很慢也能较快)。
6. **手机优先 + 横竖屏可选**:**提词播放页**必须**黑底白字**、大字号、高对比(黑底防反光玻璃眩光)。点「开始提词」后由用户选择**横屏 / 竖屏**进入,两种朝向同一套滚动逻辑;朝向与设备不符时提示旋转。
7. **提词时屏幕常亮**:提词滚动期间屏幕不息屏。
8. **文字宽窄(左右边距)可调**:提词页可实时缩窄文字栏(加大左右边距),避免远距离念稿时眼睛大幅左右扫视。
9. **进入先调设置 + 可选倒计时**:进入提词**静止待命**(不自动播放);设置集中在**弹出式设置面板**里(镜像 / 提示线开关 / 倒计时档位 / 速度 / 字号 / 宽窄 / 重置);点「开始」按所选倒计时(关闭/3/5/10 秒)再滚动。倒计时仅首次开始,继续不倒计时。
10. **极简提词界面**:正文几乎占满全屏,操作收进**右上角 4 个图标(返回 / 设置 / 开始 / 暂停)**与按需弹出的设置面板;不再有占比过大的底部控制栏。详见 `HANDOFF.md` §6。

---

## 2. 明确不做的部分(第二版以后,现在一律不要碰)

- ❌ 语音识别、语音跟读、念到哪滚到哪
- ❌ AI 生成稿子 / AI 润色
- ❌ 任何后端、服务器、API key、账号系统
- ❌ GitHub 部署、完整 PWA(manifest / service worker / 离线缓存)
- ⚠️ 例外:为在 iOS 上实现真全屏,`<head>` 保留了 `apple-mobile-web-app-capable` 等少量 meta 标签,使「添加到主屏幕」后能以独立全屏运行。这**不是完整 PWA**(无 manifest、无 service worker、无离线),仅为去掉 iOS 浏览器外壳,用户 2026-06-02 确认需要。
- ❌ 提词页亮/暗主题切换开关(第一版固定黑底白字)、垂直镜像等额外选项
- ❌ 任何未在「功能范围」中列出的功能,不要自作主张添加

---

## 3. 技术约束(重要,严格遵守)

- **技术栈:单个 HTML 文件,内联 CSS + 原生 JavaScript。不引入任何框架**(不要 React/Vue),
  无构建步骤,无 npm 依赖。目标:双击就能在浏览器打开。
- **持久化仅限稿子文本一项**,用 `localStorage`。字号/速度/镜像等设置**不持久化**,每次用默认值。
  其余一切运行时数据用内存中的 JS 变量/状态。
- **代码清晰、易读、注释充分**(将开源,且会有别的 AI 工具接力开发)。
- 移动端横屏的视口、字体缩放、滚动性能要专门处理好,**滚动必须流畅不卡顿**。

---

## 4. 已确认的产品决策

| 决策点 | 结论 |
|---|---|
| 界面结构 | **双模式切换**:默认「编辑模式」贴稿;点开始进入全屏「提词模式」,整屏只有滚动文字 + 极简控制栏。 |
| **屏幕方向** | **编辑主页:竖屏优先**(手机正常竖握即可写稿,横竖屏都能用)。**提词页:强制横屏**,竖屏时才盖"请横屏"提示。**提示遮罩只在提词模式生效,绝不遮挡编辑主页。** |
| 阅读基准线 | **要**。提词时屏幕**垂直中部**一条淡线 / 半透明高亮带,标记当前朗读行。 |
| 稿子持久化 | **要,仅存稿子文本**到 localStorage,刷新自动恢复。其他设置不持久化。 |
| 配色范围 | **提词播放页:黑底白字**(纯黑背景纯白大字,高对比、防反光眩光)。**主界面(编辑模式):由架构师单独设计**(深色简约风,见 §10)。 ⚠️ 原定白底黑字,用户于 2026-06-02 评审后改为黑底白字(理由:反光玻璃提词场景防眩光、对比最高)。 |
| 屏幕常亮 | **第一版要做**,用 Screen Wake Lock API,提词时常亮。 |

---

## 5. 文件结构

第一版**就是单个 `index.html`**(内联 CSS + 内联 JS)。开源相关文件可后补。

```
teleprompter/
├── index.html       # 全部:HTML 结构 + 内联 <style> + 内联 <script>
├── ARCHITECTURE.md  # 本文档
├── README.md        # (可选,开源时补)用途、用法、第二版 roadmap
└── LICENSE          # (可选,开源时补)如 MIT
```

### `index.html` 内部组织(用注释分块,便于接力)

```
<style>  按区块组织:
  1. 变量与重置        :root 自定义属性 / box-sizing / 去默认 margin
  2. 横屏基础布局      100dvh、Flex、竖屏提示遮罩
  3. 编辑模式 .editor  主界面独立配色(深色简约)
  4. 提词模式          .prompter / .viewport / .scroller / .guide(黑底白字)
  5. 控制栏 .controls  提词模式底部极简控制栏

<body>  两个模式各一个顶层容器,靠 class 切换显隐

<script>  按模块组织(职责单一,互不耦合):
  1. state         单一状态对象
  2. dom           元素引用集中获取
  3. storage       仅稿子文本的 load/save(try/catch 降级)
  4. scrollEngine  requestAnimationFrame 滚动循环
  5. wakeLock      屏幕常亮:申请/释放/回前台重申请
  6. controls      各控件事件绑定 + 实时调参
  7. mode          编辑/提词 进入与退出
  8. init          启动装配
```

---

## 6. 状态模型

所有运行时数据集中在一个对象,UI 只读它、改它。**只有 `text` 持久化。**

```js
const state = {
  text:       '',        // 稿子文本 —— 唯一持久化项(localStorage)
  mode:        'editor',   // 'editor' | 'prompter'
  orientation: 'landscape',// 'landscape' | 'portrait',进入提词时用户选择(见 HANDOFF §6.4)
  phase:       'ready',    // 提词播放子状态:'ready'|'counting'|'playing'|'paused'(见 §7.11)
  isPlaying:   false,
  isMirrored:  false,
  showGuide:   true,       // 提示线(中部基准线)开关
  settingsOpen:false,      // 设置面板是否打开
  fontSize:    64,         // px,默认值,打开即可用
  speed:       40,         // px/秒,内部单位
  sideMargin:  7,          // vw,文字栏左右内边距(越大越窄),见 §7.10
  countdownSec:5,          // 0|3|5|10,0=关闭(立即开始)
  position:    0,          // 当前滚动位移(float, px),负值向上
};
```

**默认值(打开即用,不调也能念):** 字号 64px、速度约 40px/s、宽窄 7vw、镜像关、首行停在基准线附近。
**实现细节见交接文档 `HANDOFF.md` §5(宽窄控件 + 倒计时状态机的精确改法)。**

---

## 7. 关键技术方案

### 7.1 横屏布局

- **视口 meta**:`width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no`
  —— 禁止双指缩放打乱布局(字号用自带控件调,不靠系统缩放)。
- **高度用 `100dvh`**(dynamic viewport height),自动避开手机浏览器地址栏伸缩,比 `100vh` 稳。
- **整体 Flex**;提词模式正文区占满中间,控制栏是底部一条细栏。
- **竖屏提醒只作用于提词模式**:用 `body[data-mode="prompter"]` 限定,`@media (orientation: portrait)` 下才盖半透明遮罩提示"请横屏使用",横屏自动消失。**编辑主页在竖屏下正常可用,绝不被遮挡。** JS 在进入/退出提词时切换 `document.body` 的 `data-mode` 属性。
  Screen Orientation Lock API 在非全屏下基本锁不住,**第一版不依赖它,只做提示**。
- **进入提词模式时可选 `requestFullscreen()`**:由"开始"按钮点击手势触发,隐藏浏览器外壳。失败静默降级。

### 7.2 主界面(编辑模式)配色

- **主界面配色独立,无需跟随提词页**,单独设计。建议:深色背景(如 `#1a1a1a`)+ 浅色文字 + 一个明显的主操作「开始」按钮。
- 与提词页(纯黑沉浸式播放)同属深色系、风格统一,又靠品牌色/渐变/字号层次区分,且更省眼。
- textarea 保持足够留白与大点击区,适合手机横屏贴稿。
- 实现上主界面与提词区本就是两个独立容器,各自一套配色 class,互不影响。

### 7.3 镜像翻转(核心)—— CSS `transform: scaleX(-1)`

**关键:镜像与滚动位移分到两个不同元素,避免 transform 互相覆盖/顺序踩坑。**

```
.viewport          ← 裁剪窗口;镜像开关切换它的 class(scaleX(-1))
   └─ .scroller    ← 真正滚动的正文;只承载 translateY 位移
.guide             ← 阅读基准线,是 .viewport 的「兄弟」覆盖层,不进镜像
```

- 镜像作用在外层 `.viewport`,滚动 `translateY` 作用在内层 `.scroller`,两者互不干扰
  → 一键开关镜像时滚动数学完全不用改。
- 阅读基准线是水平线,放在镜像容器**之外**,镜像与否不翻转、不移位。
- 一行 class 切换即可实时开关,无重排成本。

### 7.4 匀速滚动 —— `requestAnimationFrame` + 时间增量(delta-time)

不要用 CSS animation,也不要用 `scrollTop`。

- 每帧:`position -= speed * dt`(`dt` = 本帧与上一帧真实时间差,秒)。
  **用 dt 而非"每帧固定像素",保证不同刷新率手机(60/90/120Hz)上速度一致、真正匀速。**
- 位移用 **`transform: translate3d(0, position, 0)`** 应用到 `.scroller`,开 GPU 合成层
  (配 `will-change: transform`),滚动顺滑。
- **避免每帧读取布局**(如 `offsetHeight`)造成 reflow:正文总高度只在
  「文本变化 / 字号变化 / 进入提词模式 / 窗口尺寸变化」时重算并缓存。
- **暂停** = 停止 rAF 并记住 `position`;**继续** = 用当前 `position` 重启循环;
  **重置** = `position` 归位到起始点(首行落在基准线附近)再重画。
- 滚动到底部后停止(不循环);可在底部留一段空白便于念完最后一行。

### 7.5 速度实时可调 —— 指数映射

- 滑块 0–100 **指数映射**到 px/秒(如 `speed = base * Math.pow(ratio, t)`),
  低速端细腻(能很慢)、高速端够快,满足"范围足够宽"。
- rAF 每帧现读 `state.speed`,**拖动滑块即时变速,无需重启循环**。

### 7.6 字号实时可调

- `.scroller` 的 `font-size` 绑定 `state.fontSize`,加减按钮(或滑块)直接改值并写回样式。
- rAF 读的是当前 `position`,字号变了仍连续滚动,无需重启;改后**触发一次内容高度重算**。
- `line-height` 用相对值(如 1.4),随字号缩放。

### 7.7 屏幕常亮 —— Screen Wake Lock API

- `navigator.wakeLock.request('screen')`,**进入提词 / 开始滚动时**申请,**退出提词 / 关闭页面时**释放。
- 由"开始"按钮的用户手势触发,满足浏览器调用要求。
- **必须处理 `visibilitychange`**:切后台再回前台,锁会自动失效,需**重新申请**(标准坑)。
- 包 try/catch:不支持的浏览器静默降级,不阻断提词。

### 7.8 持久化(仅稿子)

- 编辑区 `input` 事件(防抖)把 `state.text` 写入 `localStorage`;启动时读回填充。
- 读写包 try/catch,隐私模式禁用存储时静默降级为纯内存。
- **不要持久化字号/速度/镜像等任何其他设置。**

### 7.9 提词界面:右上角图标 + 弹出式设置面板(第三轮重构,以此为准)

> 早期版本用"底部控制栏",真机实测占屏比例过大,已废弃。现行范式见 `HANDOFF.md` §6。

- **正文几乎占满全屏**;操作收进**右上角 4 个图标**:返回 / 设置 / 开始 / 暂停。
- **设置面板(深色玻璃)**集中所有可调项:镜像、提示线开关、倒计时档位(关闭/3/5/10)、速度、字号、宽窄、重置。
- 进入提词**自动弹面板**;点面板外关闭进入干净视图;播放中点「设置」自动暂停。
- 面板内滑块实时生效;改字号/宽窄后必须 `calculateBounds()`。

### 7.10 文字宽窄(左右边距)实时可调

- 用 `.scroller` 的 CSS 变量 `--side-pad`(默认 `7vw`)控制左右内边距,滑块实时改;数值越大文字越窄。
- 改宽窄会改变换行、影响内容高度,**改后必须 `calculateBounds()` 重算滚动边界**(同字号)。
- 不持久化。精确改法见 `HANDOFF.md` §5-A。

### 7.11 进入待命 + 5 秒倒计时开始流程

- 提词播放有四态 `phase`:`ready`(进入/重置后静止待命)→ `counting`(5 秒倒计时)→ `playing` → `paused`。
- 进入提词**不自动播放**,先让用户调镜像/字号/宽窄/速度;点「开始」才进入 `counting`,全屏显示 5→1,结束后自动滚动。
- **倒计时仅 `ready→playing` 首次触发**;`paused→playing`(继续)立即开始、不倒计时;`reset` 回到 `ready`。
- 倒计时浮层 `pointer-events:none`,点击穿透到正文以便「取消」;倒计时期间也持有 Wake Lock。
- 退出/重置必须清掉倒计时定时器,严防离开后仍在后台计时。精确状态机与改法见 `HANDOFF.md` §5-B。

---

## 8. 分步开发清单(小步推进,每步可独立验证)

> Codex 请按此顺序实现,每步做完先自测「如何验证」一栏再进入下一步。

| 步骤 | 做什么 | 如何验证 |
|---|---|---|
| **0. 骨架** | `index.html` + viewport meta + `100dvh` 基础布局 + 竖屏提示遮罩 + `:root` 变量 | 手机横屏打开满屏正常;转竖屏出现"请横屏"提示 |
| **1. 编辑模式** | 主界面(深色简约)+ 文本域 + "开始"按钮;`storage`:输入防抖存稿、启动读回 | 贴入文字 → 刷新页面 → 文字仍在 |
| **2. 模式切换** | 编辑↔提词 容器显隐;进入时把 `text` 渲染进 `.scroller`;可选全屏 | 点"开始"进入全屏提词(黑底白字);退出回编辑且文字不丢 |
| **3. 滚动引擎** | rAF + dt 累加 + `translate3d`;开始/暂停/继续/重置 | 文字匀速上滚;暂停定住;继续接着走;重置回开头 |
| **4. 速度实时调** | 滑块 → 指数映射 → `state.speed`,每帧现读 | 拖动滑块立刻变速,能很慢也能较快 |
| **5. 字号实时调** | 加减/滑块改 `font-size` + 重算缓存高度 | 滚动中改字号,字立即变化且继续滚 |
| **6. 镜像翻转** | `.viewport` 切 `scaleX(-1)` class | 一键开关,文字左右翻转,滚动不受影响 |
| **7. 阅读基准线** | `.viewport` 外层加居中横线/半透明高亮带覆盖层 | 屏幕中部有线;镜像开关时线不翻转、不移位 |
| **8. 控制栏与手势** | 底部极简控制栏整合全部控件;点正文=播放/暂停;退出按钮;样式打磨 | 单手可操作;视觉极简克制 |
| **8.5 屏幕常亮** | 进入提词/开始时申请 Wake Lock,退出释放;`visibilitychange` 回前台重申请;try/catch 降级 | 提词时放着不动屏幕不息屏;切后台回来仍常亮;旧机型不报错 |
| **9. 性能与收尾** | `will-change`、去每帧重排、节流 resize;补全注释;多机型测试 | 90/120Hz 手机滚动顺滑匀速;代码注释清晰可接力 |

---

## 9. 验收标准(第一版完成的定义)

- [ ] 单个 `index.html`,双击即开,无任何外部依赖、无构建步骤。
- [ ] 手机横屏:贴稿 → 开始 → 匀速上滚 → 暂停/继续/重置 全部正常。
- [ ] 镜像开关一键生效,文字左右翻转且滚动不乱。
- [ ] 字号、速度、**文字宽窄(左右边距)** 均可在提词页实时调整。
- [ ] 点「开始提词」先进入静止待命页可调设置;点「开始」后 5 秒倒计时再滚动(仅首次,继续不倒计时)。
- [ ] 提词播放页黑底白字(纯黑背景白字)、大字号、高对比;主界面为独立(深色)设计。
- [ ] 提词时屏幕保持常亮,切后台回前台仍常亮。
- [ ] 稿子文本刷新后自动恢复;无其他持久化。
- [ ] 中高刷新率手机上滚动流畅不卡顿。
- [ ] 代码注释充分、结构清晰,便于开源与后续接力。

---

## 10. 编辑主页设计规范(架构师设计,已落地 index.html)

> 编辑主页由架构师直接设计并写入 `index.html`。Codex 改其它逻辑时**请勿改动此视觉规范**,
> 如需调整先与架构师对齐。提词页(黑底白字)不属于本规范,保持纯黑高对比不变。

### 设计令牌(design tokens)
| 用途 | 值 |
|---|---|
| 主背景 | `radial-gradient(130% 90% at 50% -15%, rgba(33,192,138,0.16), transparent 60%)` 叠加 `linear-gradient(180deg,#101218,#0c0d11)` |
| 正文文字 | `#f5f7fa` |
| 次要文字/提示 | `#8b93a7` |
| 强调色 Accent | `#21c08a` → `#16a877`(渐变) |
| 纸面(textarea 容器) | `rgba(255,255,255,0.05→0.02)` 渐变 + `1px rgba(255,255,255,0.08)` 边 + 大投影 |
| 圆角 | 纸面 18px / 按钮 14px / 品牌标 11px |
| 字体 | 系统栈(无网络字体,保证离线双击可用) |

### 布局(竖屏优先,列布局)
1. **顶部品牌栏**:左侧品牌标(绿色圆角方块 + 上滚文字 SVG 图标)+「提词器」;右侧「草稿自动保存」胶囊(呼应 localStorage 持久化,**非新功能,仅信息提示**)。
2. **标题区**:`写好稿子,<em>横屏开念</em>`(强调色)+ 一行说明。横屏时隐藏以让出纵向空间。
3. **纸面输入区**:`flex:1` 占满主空间,聚焦时边框点亮强调色 + 光晕;右下角字数统计(**轻量辅助,非新功能**)。
4. **底部主操作**:整宽「▶ 开始提词」按钮,强调色渐变 + 柔和发光,按压下沉反馈。

### 横屏适配
编辑页横屏时:隐藏大标题、压缩内边距、按钮降高,保证短纵向空间下输入区仍充足。

### 备注(可选项,用户可一键移除)
- **字数统计** 与 **「草稿自动保存」胶囊** 是提升观感的微交互,只读现有状态、未引入新功能。
  若判定超出第一版范围,删除对应 DOM/CSS/`updateCount` 即可,不影响任何核心逻辑。
